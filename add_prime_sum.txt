given a num we need to find all primers and sum them

1. lib unistd
2. ft_atoi
3. ft_putnbr
	check if n > 9
		if yes recursive func with n divided on 10
	write one sign from string with digits with n%10
4. is_prime
	create i equal 2
	check if num less or equal 2
		return 0
	loop while square of i less or equal num
		check if n % i == 0
			if yes return 0
			if not increase i
	return 1
5. main
	create sum
	check if ac==2
	create num with ft_atoi
	loop till n > 0
		check if is prime
			if yes add num to sum
			increae num
	print num
	if ac != 2
		print 0