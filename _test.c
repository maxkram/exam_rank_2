#include <stdio.h>
#include <string.h>
#include <unistd.h>

int is_space(char c)
{
	return (c == 32 || c == 9);
}

int main(int ac, char **av)
{
	if (ac == 2)
	{
		int i = 0;

	}
	write(1, "\n", 1);
	return (0);
}
