make int from str with different base

create int res=0 and sgn=1

while *str is empty spaces
	increase str
if *str minus sign
	make sgn = -1
else if *str - or +
	increase str
while loop with str
	create int digit
	if str number
		dig equals this str minus '0'
	else if str between a and f
		dig equals str minus a and plus 10
	else if str between A and F
		dig equals str minus A and plus 10
	else
		break;
	
	if digit more or equal base
		break
	
	res equals res times base + digit
return digit * sgn