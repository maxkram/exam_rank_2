include stdlib for malloc

int function
	create int *r (array)
	create len
	if end more or equal start
		len equals end minus start
	else
		len equals start minus end
	r equal malloc of len size
	if no r
		get NULL
	while len exists
		if end more or equal start
			r[len] = start++
		if not
			r[len] = start
		decrease len
	return r
