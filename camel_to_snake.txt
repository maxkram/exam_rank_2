givenAStrNeedTo_change_to_camel

lib unistd
main
	check if ac==2
		while av exist
			check if av is capital letter
				if yes add 32 of ascii
				write "_"
			after write this av (already changed or not)