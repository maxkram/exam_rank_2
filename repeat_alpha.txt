lib unistd

void repeat_alpha
create int vars letter and i equal 0
while i of str
    letter equal 0
    if (i of str is capital letter)
        letter equal is this i minus 'A' - 1
    else if (i of str is minuscule letter)
        letter equal is this i minus 'a' - 1
    else
        letter equal 1
    while letter exists
        write this i of str
        decrease letter
    increase i